#!/usr/bin/env bash
set -euo pipefail
HERE="$(cd "$(dirname "$0")" && pwd)"
ROOT="$(cd "$HERE/.." && pwd)"
source "$ROOT/.venv/bin/activate"
if [ -f "$ROOT/.env" ]; then set -o allexport; source "$ROOT/.env"; set +o allexport; fi
exec python "$ROOT/src/reel.py" "$@"
